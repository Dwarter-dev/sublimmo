{% extends 'base.html.twig' %}

{% block title %}Hello PayementController!{% endblock %}

{% block body %}

<main>

  <div class="row"> <!-- On affichera les messages d'erreurs-->
    <div class="col-8 offset-2">
      {% for label, messages in app.flashes %}
          {% for message in messages %}
              <div class="flash-{{ label }} bg-{{ label }} text-light p-3 mb-5 rounded">
                  {{ message }}
              </div>
          {% endfor %}
      {% endfor %}
    </div>
  </div>

  <div class="row">
    <div class="col-8 offset-2">
      <h2 class="mb-5">Payement accepté</h2>
      <p>Le paiement a été refusé. Merci de réessayer ultérieurement.</p>
      <p>Si le problème persiste, merci de contacter un administrateur du site via le <a href="{{ path('contact') }}" class="text-primary">Formulaire de contact</a>.
      <a href="{{ path('cart_index') }}" class="text-success"> Retour au panier</a></p>
    </div>
  </div>

</main>
{% endblock %}
